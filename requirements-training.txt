# Core ML Libraries (CPU-only versions are much smaller)
# CPU-only PyTorch: ~600MB instead of 2.5GB
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.0.1
torchvision==0.15.2
numpy==1.24.3
pandas==2.0.3
Pillow==10.0.0

# Experiment Tracking
mlflow==2.7.0

# Monitoring & Logging
python-json-logger==2.0.7

# Utilities (minimal, training only)
requests==2.31.0
pyyaml==6.0.1
